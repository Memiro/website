# Local development config for host-side tools (`just run`, `just migrate`).
# The db service publishes 5432 on loopback for exactly this file.

[db]
host = "localhost"
port = 5432
user = "memiro"
password = "memiro"
database = "memiro"

[observability]
enabled = false

[email]
# The manager channel: the app password lives in ``smtp_password`` next to
# this file, outside git; without that file the channel stays silent locally.
enabled = true
host = "smtp.yandex.ru"
port = 465
username = "memiro.ru@yandex.ru"
password_file = "smtp_password"
from_address = "memiro.ru@yandex.ru"
manager_address = "memiro.ru@yandex.ru"
timeout_seconds = 10.0

[legal]
# Редакция текста политики обработки ПДн: правится тем же коммитом, что и сам
# текст, — иначе согласие в заявке перестаёт быть доказательством (ADR-0006).
consent_version = "2026-08-31"

[admin]
# Ключ подписи сессий админки. Локальное значение — заведомо не секрет;
# в деплое файл конфигурации заполняет владелец вне git.
secret_key = "dev-only-not-a-secret"
allowed_hosts = ["localhost", "127.0.0.1"]

[media]
# Фотографии товаров: локально — папка в корне репозитория, в контуре — том,
# который отдаёт nginx. Путь адаптера, в базе от него нет ничего (тикет 08).
root = "media"
