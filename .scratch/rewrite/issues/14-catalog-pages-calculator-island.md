# 14: Каталог, карточка и остров калькулятора

**What to build:** Покупатель открывает быстрые серверные страницы каталога и карточки (user story 18) и настраивает зеркало в калькуляторе: остров Vue (`client:visible`) шлёт выбор на `POST /api/calculate` и показывает цену сразу — итог и дельты выбранных добавок («подсветка +11 500 ₽») без устройства цены (ADR-0005, ADR-0007, user stories 13–15). Калькулятор открывается на первом подходящем предпосчитанном варианте (решение 62). Погашенная цена — карточка без цены, но с конструктором (ADR-0008). Вердикты отображаются: за пределом производства — приглашение оставить пожелание, а не отказ. Состояние калькулятора живёт в его компоненте.

**Blocked by:** 12 (`browse_catalog` API), 13 (скелет фронта)

**Status:** ready-for-human

- [x] Страницы каталога и карточки рендерятся SSR по слагам из API
- [x] Остров калькулятора считает на сервере по актуальным тарифам; цена совпадает с ответом эндпоинта
- [x] Дельты добавок показаны; ставки/коэффициенты/статьи нигде не всплывают
- [x] Калькулятор открыт на первом подходящем варианте; переключение варианта меняет конфигурацию
- [x] Вердикты BEYOND_LIMITS и HIDDEN имеют внятное отображение

## Comments

- Реализовано 31.08.2026. Локально прошли `npm run test`, `npm run check`,
  `npm run build`, `just lint`, `just static` и `just test` (372 passed).
  `just test-e2e` трижды не дошёл до тестов: Docker-сборка зависла на внешнем
  шаге `frontend: npm ci`; требуется повторить проверку контура после
  восстановления доступа к npm registry и визуально сверить страницы с
  legacy Django-витриной.
