# 05 — Убрать акции с витрины

**What to build:** Блок акций уходит с главной и бейдж «Акция» — с плиток.
Модель и флаг остаются в админке нетронутыми.

**Why:** Владелец: «акции пока убираем, продумаем позже и добавим». Удалять
модель ради временного скрытия — значит писать её заново через месяц. К моменту
возврата акции переделываются: вместо «специальных цен» блок будет называть
размер скидки в процентах (пункт 2 списка, отложен вместе с акциями).

**Blocked by:** None.

**Status:** ready-for-human

- [x] Блок акций убран с главной (`templates/home.html`)
- [x] Бейдж «Акция» убран с плитки (`templates/catalog/_card.html`) и со
      страницы товара
- [x] Выборка акционных товаров убрана из `views.py`
- [x] Модель `content.Promo`, флаг `Product.is_promo` и их разделы админки
      остаются на месте — миграций нет
- [x] Тесты главной не ждут блока акций

**CONTEXT.md:** термин «Акция» остаётся, но перестаёт описывать блок на главной.
Определение поправить: акция публикуется в админке и на витрине сейчас не
показывается.

## Comments

**2026-08-24 (agent).** Сделано. Акция снята с витрины, из админки не тронута.

- Блок акции удалён из `templates/home.html`; дисклеймер «не публичная оферта»
  под лентами теперь висит на одном условии `{% if popular %}` — цены на главной
  остались только там.
- Бейдж «Акция» убран из `templates/catalog/_card.html` и
  `templates/catalog/product.html`.
- `views.home()` больше не читает `Promo` и не набирает ленту по `is_promo`:
  ушли два запроса и импорт модели.
- Правило `.badge` из `static/css/site.css` удалено следом за единственными
  двумя его носителями. Возвращать акции придётся не этим стилем: блок
  переделывается под размер скидки (issue 23), и мёртвое правило до тех пор
  читалось бы как «бейдж где-то ещё есть».
- Модель `Promo`, флаг `Product.is_promo`, разделы админки и миграция
  `content/0003_promo_for_existing_sale` — на месте. Миграций нет.

Тесты переписаны с «показывается» на «не показывается», а не удалены: скрытие
временное, и стеречь его должно что-то. Опубликованная акция + товар с флагом
не дают на главной ни заголовка, ни ленты, ни бейджа
(`tests/test_marketing.py`, `tests/test_home.py`); флаг не метит и плитку
каталога с карточкой (`tests/test_catalog_pages.py`). Тест переезда
`0003` теперь проверяет саму запись в базе, а не блок на главной — данные
переезда живы, видимости у них нет. Админский путь сохранён отдельным тестом:
акция правится и остаётся опубликованной, на витрину не выходит.

`CONTEXT.md`: статья «Акция» переписана — предложение заводится в админке,
на витрине не показывается, вернётся с размером скидки.

Проверки: 378 тестов, ruff, mypy, bandit — зелёные. `manage.py check` ругается
только на незаполненные реквизиты продавца (открытый пункт тикета 10 набора
`new-site`), к этой правке отношения не имеет.

**2026-08-24 (agent, ревью).** Три правки по итогам разбора.

- **Админка врала владельцу.** Подсказка в форме акции продолжала обещать:
  «на главную выходит первая опубликованная акция, товары набираются флагом
  «акция»». Это единственное место, где снятие блока видно пользователю:
  владелец публикует акцию, ставит флаги товарам и не находит их на сайте.
  Текст переписан — заведённое сохраняется и выйдет, когда акции вернутся.
- **Докстринг `Promo`** остался в дотикетном мире и ссылался на `CONTEXT.md`,
  который уже поправлен. Приведён к нему.
- **Тест без зубов.** `test_home_hides_empty_popular_section` проверял
  `"Популярное" not in content`, а шаблон печатает «Популярные товары» —
  подстрока не встречалась на странице никогда, и покраснеть тест не мог.
  Раньше его держала соседняя проверка на «Акция», а она из этого теста
  уехала. Теперь сверка идёт по настоящему заголовку.
- Два подряд `{% if popular %}` в `home.html` схлопнуты в одно.

**Чего не сделано и почему:** флаг «акция» у товара правится прямо в списке
админки и своей подсказки не получил. `help_text` у поля — это `AlterField`,
то есть миграция, а тикет требует «миграций нет»; заводить `fieldsets` у
`ProductAdmin` ради одной строки — переделывать форму товара из тикета про
акции. Владелец прочитает то же самое в разделе «Акции», куда он приходит
раньше.

**2026-08-24 (agent, двухосевое ревью).** Ось Spec не нашла ни пропущенных
пунктов, ни неверных реализаций: скрытие полное (проверены `seo/sitemaps.py`,
`seo/structured.py`, фильтры каталога, `shop.js`), сохранённое сохранено.
Три пункта scope creep — удаление `.badge`, правка подсказки в `PromoAdmin`,
починка мёртвого ассерта — оставлены как есть: они уже разобраны выше, и
откат каждого вернул бы либо мёртвый код, либо ложь владельцу.

Ось Standards нашла настоящую дыру, и она починена:

- **ADR-0004 не знал о расхождении.** ADR держит реестр отступлений от
  handoff-пакета, «которые не должны „чиниться“ обратно», а блок акции
  в пакете нарисован: надзаголовок «Акция · весенние скидки» над
  «Популярными товарами» и подпись «весенняя скидка» на карточке
  (`design/mockups-v2/HANDOFF.md`). Записанное только в `.scratch/` не
  спасло бы: следующий агент, сверяющий главную с макетом, увидел бы
  недоделанную вёрстку и вернул блок. Расхождение добавлено в ADR
  отдельным абзацем — не пятым пунктом списка: те четыре следуют из
  приоритета «закон и SEO выше попиксельной верности», а это решение
  владельца, и смешивать их значило бы соврать о причине.
- **`CONTEXT.md` ссылался на номер тикета** — единственная такая строка в
  глоссарии, остальные статьи держатся за ADR и имена в коде. Скретч живёт
  до чистки, глоссарий дольше; ссылка переведена на ADR-0004.
- Тест `test_promo_flag_gives_no_badge` в `test_catalog_pages.py`
  переименован в `..._in_catalog`: от соседа в `test_marketing.py` он
  отличался суффиксом `_on_home`, и различить их по имени было нельзя.

Дублирование тестов, отмеченное как суждение, оставлено: три проверки
«Dew Glow не виден» стерегут три разных пути — опубликованную акцию,
правку через админку и переезд миграции, — а два теста про бейдж ходят
по главной и по каталогу.

Проверки после правок: 378 тестов, ruff, mypy, bandit — зелёные.
