// The revision of the privacy text. It is stored with every inquiry as proof
// of consent, so it changes in the same commit as the text on /privacy/ and
// must match `consent_version` in the backend config.
export const CONSENT_VERSION = "2026-08-31";
