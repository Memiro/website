export const SITE_NAME = "Memiro";

// The preview a messenger unfolds a link into: 1200x630, the proportion every
// unfurler expects. It is deliberately kept out of the build pipeline — a
// hashed name changes with every build, and a preview address has to outlive
// them all.
export const OG_IMAGE = "/img/og-cover.jpg";
